[tool.poetry]
name = "iar-api"
version = "0.0.2-SNAPSHOT"
description = "Service Web d'Indexation Sujet RAMEAU (API FastAPI)"
authors = ["ABES"]
readme = "README.md"
package-mode = false

[tool.poetry.dependencies]
python = "^3.10"
